import styles from 'styles/app/notFound.module.css';

const Loading = () => {
    return (
        <main className={styles.main}>
            <p>読み込み中...</p>
        </main>
    );
};

export default Loading;
